-- Set your webhook, webhook delay, and boxtrain settings
getgenv().boxtrain = "false" -- Change to True if your Alts Are Box Trained

-- Load the first script
loadstring(game:HttpGet("https://raw.githubusercontent.com/Username/Repository/Branch/Script1.lua", true))()

-- Load the script to destroy the map
loadstring(game:HttpGet("https://raw.githubusercontent.com/Username/Repository/Branch/Script2.lua"))()

-- Load the premium GUI script
loadstring(game:HttpGet("https://raw.githubusercontent.com/Username/Repository/Branch/Script3.lua"))()

-- Check if boxtrain is true
if getgenv().boxtrain == "true" then
    -- Load the boxing farm script
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Username/Repository/Branch/Script4.lua"))()

    -- Additional modifications for boxing farm
    local targetPosition = Vector3.new(-277.3309020996094, 23.595672607421875, -1125.0675048828125)

    -- Wait for the player to teleport
    wait(2)

    -- Find the item in the shop
    local itemName = "Boxing Moveset (Require: Max Box Stat) - $0"
    local itemToBuy = workspace.Ignored.Shop:FindFirstChild(itemName)

    if itemToBuy then
        -- Click the item's ClickDetector
        fireclickdetector(itemToBuy.ClickDetector)
    else
        warn(itemName .. " not found in the shop.")
    end
end

-- Load the final script
loadstring(game:HttpGet("https://raw.githubusercontent.com/Username/Repository/Branch/Script5.lua"))()
